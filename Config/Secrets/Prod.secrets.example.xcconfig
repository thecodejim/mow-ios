# Copy to Prod.secrets.xcconfig when preparing App Store / production builds.
# Only override values that must stay outside of source control.
# Example:
#   HEALTH_CHECK_TOKEN = prod-super-secret-token
